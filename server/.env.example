# Local dev — Node API (see server/README.md). Default PORT=8001.
# Password must URL-encode special chars (e.g. @ → %40). Supabase direct connection; sslmode required for remote.
DATABASE_URL=

SECRET_KEY=
ALGORITHM=
ACCESS_TOKEN_EXPIRE_MINUTES=

HF_API_TOKEN=
HF_TEXT_MODEL=
HF_VISION_MODEL=

FRONTEND_URL=

# Python sign-inference microservice (Phase 1). Default local port 9001 — see services/sign-inference/README.md
SIGN_SERVICE_URL=

PORT=
HOST=
